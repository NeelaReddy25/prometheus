groups:
- name: DiskUsageHigh
  rules:
  - alert: DiskUsageHigh
    expr: 100-(node_filesystem_avail_bytes{mountpath="/"}*100 / node_filesystem_size_bytes{mountpath="/"}) > 0.3
    for: 1m
    labels:
      sevrity: critical
    annotations:
      summary: Disk Usage is High
